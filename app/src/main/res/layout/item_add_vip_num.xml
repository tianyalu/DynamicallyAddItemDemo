<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:layout_marginTop="1dp"
    android:background="@color/white"
    android:gravity="center_vertical"
    android:focusableInTouchMode="true"
    android:focusable="true"
    android:paddingLeft="16dp"
    android:paddingRight="0dp">
<!--加上以上两个属性防止刚进入Activity时EditText抢占焦点-->

    <TextView
        android:id="@+id/tv_index"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="12dp"
        android:text="1"
        android:textColor="@color/blackFont" />

    <EditText
        android:id="@+id/et_vip_number"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:background="@null"
        android:hint="请输入会员号码"
        android:inputType="number"
        android:padding="4dp"
        android:textSize="14sp" />

    <!--给ImageView设置padding效果不明显，所有加上一个布局以扩大点击范围-->
    <LinearLayout
        android:id="@+id/ll_delete"
        android:layout_width="48dp"
        android:layout_height="match_parent"
        android:clickable="true"
        android:background="@drawable/bg_default_item_selector"
        android:gravity="center">
        <ImageView
            android:id="@+id/ib_delete"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:background="@drawable/item_delete"
            android:padding="14dp" />
    </LinearLayout>

</LinearLayout>